(function () {
    'use strict';

    angular.
        module('ux.strategy').
        controller('strategyCtrl', strategyCtrl);

    strategyCtrl.$inject = ['$scope'];

    function strategyCtrl($scope) {

    }
})();
